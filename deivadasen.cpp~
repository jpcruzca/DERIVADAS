#include<iostream>
#include<cmath>
int main()
{
  double x;
  double h;
  cin >> a;
  std::cout << fsev(a,h) << std::endl; 
}
double fdev(double x, double h)
{
  return (std::sin(x+h)-std::sin(x))/h;
}

